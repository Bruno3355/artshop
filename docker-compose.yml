services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_DATABASE: artshop_db
      MYSQL_ROOT_PASSWORD: fortknox
    ports:
      - '3306:3306'
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: